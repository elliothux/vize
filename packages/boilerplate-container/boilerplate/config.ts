const info = {
  name: '{{name}}',
  desc: '{{desc}}',
  author: '{{author}}',
};

const dataForm = {
  exampleText: {
    title: 'example text',
    default: 'hello vize!',
    type: 'string',
  },
};

export default {
  info,
  dataForm,
};
