{
  "name": "@vize/types",
  "version": "0.1.7",
  "description": "",
  "main": "dist",
  "types": "dist",
  "scripts": {
    "build": "rm -rf ./dist; tsc ./src/*.ts --outDir dist --declaration true",
    "publish": "npm run build; npm publish --access public;"
  }
}
