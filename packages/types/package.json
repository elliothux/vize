{
  "name": "@vize/types",
  "version": "0.1.6",
  "description": "",
  "main": "dist",
  "types": "dist",
  "scripts": {
    "build": "tsc ./src/*.ts --outDir dist --declaration true",
    "publish": "tsc ./src/*.ts --outDir dist --declaration true; npm publish --access public;"
  }
}
