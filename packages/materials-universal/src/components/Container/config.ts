const info = {
  name: '通用容器',
  desc: '通用容器组件，用于分组和布局等',
  author: 'vize',
  type: '*',
};

/**
 * @desc component styleSchema
 * @type {EditorComponent}
 */
export default {
  info,
  enableStyleGroup: '*',
  isContainer: true,
};
